#include<iostream>
#include<vector>

using namespace std;

void multiply(vector<int>& nums){
    int n=nums.size();

    vector<int>left(n, 1);// intitialize the left array with all as ones
    vector<int>right(n,1); // intitialize the right array with all as ones
    
    for(int i=1;i<n;i++){
        left[i]left[i]*
    }
}